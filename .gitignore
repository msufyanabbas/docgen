node_modules/
dist/
build/
+# Anchored: an unanchored "storage/" also matches
+# backend/src/modules/storage/, which is source code.
+/backend/storage/
+/storage/
.env
.env.local
*.log
.DS_Store

# Deployment credentials — server host, SSH key path, git token.
deploy.config
